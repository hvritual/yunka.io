# C10.5 — Release Qualification

## Status

- State: **Release candidate / final qualification in progress**
- Parent roadmap: issue #42
- Tracker: issue #52 / Draft PR #53
- Qualified framework predecessor: C10.4 `e8fef5f36bf1a704624ea49455894ab9ccbf9f8d`
- Qualified real consumer: `hvritual/biz@1818f1328189fbe6060ecccb8c658f2583b67cb8`
- C10.4 final joint qualification: run `33294926030` / job `99212837195`

The authoritative final C10.5 exact head and executable result are recorded in Issue #52 / PR #53 rather than written back after qualification. This avoids creating an unqualified evidence-only commit after the final run.

## Current-status authority

`docs/waves/C10-runtime-assembly-framework-productization.md` is retained as the original historical design/roadmap baseline. Its planning prose is not the current wave-status authority.

Current release status is owned by:

1. this C10.5 release record;
2. `PROJECT_MEMORY.md` for durable architecture/governance invariants;
3. the C10.1-C10.4 qualified wave records;
4. Issue #42 / #44 / #46 / #48 / #50 / #52 and PR #45 / #47 / #49 / #51 / #53 for qualification/merge tracking;
5. the real-consumer C9/C10 Framework Pressure ledgers.

C10.5 reconciled stale planning headers in the active C10.1 and C10.2 wave documents before the final candidate was qualified.

## Qualified predecessor chain

The strict dependency chain is:

1. **C10.1 Assembly Contract** — `e9c54e9657ffc64d0e56bcd869d9a7bf61e72f7c`, PR #45;
2. **C10.2 Assembly Compiler** — `f03306501ddb4a4f09e3ce0ea6f15eb23c03462e`, PR #47, based on exact C10.1;
3. **C10.3 Runtime Closure** — `561d9c779334371735acf8d394969cb752e3999d`, PR #49, based on exact C10.2;
4. **C10.4 Consumer Upgrade** — `e8fef5f36bf1a704624ea49455894ab9ccbf9f8d`, PR #51, based on exact C10.3;
5. **C10.5 Release Qualification** — PR #53, based on exact C10.4.

Real-consumer release baseline: `hvritual/biz@1818f1328189fbe6060ecccb8c658f2583b67cb8` / Draft PR #5.

C10.5 must not rewrite predecessor ownership. A release-blocking correctness defect belongs to the owning layer and must be requalified there.

## Release candidate invariants

The exact C10 release candidate must preserve:

- protobuf as the canonical writable Contract source;
- deterministic immutable AssemblyPlan facts;
- generated typed Application construction and dependency wiring;
- generated local child capabilities through canonical C9 `ExecuteChildTyped`;
- canonical C9 Executor/ExecutionScope authorization, idempotency and transaction semantics;
- `core.App` as the single lifecycle owner;
- `platform.Provider` as the App-owned shared-capability owner;
- generated explicit REST/gRPC registration only from declared bindings;
- internal-only Operations remaining transport-free;
- safe Diagnostics/Application Graph evidence;
- explicit `yunka dev --closure` ownership/readiness semantics;
- C10.4 `RuntimeBinder` as a post-Platform typed construction seam only, never a second runtime.

## Exact release gate

### A. Predecessor and fact reconciliation

- verify the C10.1-C10.4 exact heads and Git ancestry;
- verify qualified PR status and reconcile stale issue trackers after executable PASS;
- verify active C10 wave records, durable memory and Framework Pressure records agree;
- treat the original large C10 roadmap document as historical design baseline, not current status truth.

### B. Framework determinism and production qualification

On one exact C10.5 candidate:

1. run canonical framework generation (`rpc-generate`, including canonical Contract generation);
2. require no tracked generation/dependency drift;
3. run `make verify-production` with locked Go/protoc and MySQL 8.4;
4. require architecture, Contract, Assembly, C10.2, C10.3, test, race, vet, vulnerability, build and MySQL integration gates to pass;
5. require final framework tracked worktree clean.

### C. Real-consumer qualification

Against the same exact framework candidate and exact Biz C10.4 consumer:

1. run Biz `make generate` twice;
2. require byte-for-byte second-generation zero drift for Contract/Assembly generated truth;
3. run Biz `make verify`;
4. run Biz MySQL 8.4 `make pressure`;
5. re-prove internal-only Operation non-exposure, explicit permission closure, one root authorization, one root UoW/shared child scope and generated transport registration;
6. require final Biz tracked worktree clean.

### D. Architecture regression audit

Require no regression to:

- reflection DI, package scanning, plugin discovery or generic service lookup;
- package-global/second runtime ownership;
- command/listener/transport inference;
- direct transport bypass of canonical C9 Executor;
- Application-level duplicate authorization or root transaction sequencing;
- Biz-specific scope/business semantics inside Yunka.

### E. Release reconciliation

After executable PASS, update tracker metadata only:

- mark C10.1-C10.5 issue status consistent with qualification;
- update parent issue #42 with all five waves qualified;
- update PR #53 with exact candidate/evidence and ordered merge-readiness assessment;
- keep C10 PRs Draft/open until merge is explicitly authorized;
- keep deferred pressure explicitly deferred.

## Pressure disposition

C10.4 found no P0 compiler defect. `C10.4-P1-001` (post-Platform typed runtime binding) is closed by the generic `RuntimeBinder` seam and has real-consumer proof.

The only carried deferred item is historical `FP-C9-005` — safe remote Saga topology evidence in the Application Graph. It remains **OPEN / DEFERRED** because C10 produced no second independent pressure signal. C10.5 must not promote it.

## Stack merge-readiness order

If and only if merge is explicitly authorized after C10.5 qualification, preserve the dependency order:

1. PR #45 — C10.1;
2. PR #47 — C10.2;
3. PR #49 — C10.3;
4. PR #51 — C10.4;
5. PR #53 — C10.5 release reconciliation.

No merge or `main` movement is authorized by qualification itself.

## Rollback points

Before merge, immutable rollback/evidence points remain:

- C9.9 `main` release baseline;
- C10.1 `e9c54e9657ffc64d0e56bcd869d9a7bf61e72f7c`;
- C10.2 `f03306501ddb4a4f09e3ce0ea6f15eb23c03462e`;
- C10.3 `561d9c779334371735acf8d394969cb752e3999d`;
- C10.4 `e8fef5f36bf1a704624ea49455894ab9ccbf9f8d`;
- Biz C10.4 `1818f1328189fbe6060ecccb8c658f2583b67cb8`.

C10.5 may be declared **Complete / Qualified** only when the exact PR #53 head passes every gate above and tracker facts are reconciled without another source-tree mutation.
