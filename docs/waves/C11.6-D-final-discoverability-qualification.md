# C11.6-D — Final Discoverability Qualification

Status: Complete / final qualification candidate

Tracking: #94, parent #87

Base: `main@dee4c0c08f20967622ce3da4f055e562d7a256cc`

## Goal

Close C11.6 with executable proof that the developer-facing discoverability surface works through the real built Yunka CLI and that expert interfaces remain discoverable and compatible.

C11.6-D is a qualification slice. It does not add a new compiler, runtime, configuration owner, diagnostic taxonomy, command family, or deprecation policy.

## Qualified surface

The final C11.6 developer path is:

```text
yunka init
  -> yunka generate
  -> yunka check
  -> yunka dev
```

The root CLI must continue to expose four discoverability groups:

- Developer workflow;
- Diagnostics and inspection;
- Expert architecture;
- Supplementary tooling.

The current root command inventory must remain visible:

```text
api
assembly
check
contract
dependency
dev
doc
doctor
domain
explain
generate
graph
init
inspect
module
```

Expert roots remain behavior-compatible and visible:

- `contract` — check, diff, generate, inspect, lint;
- `assembly` — check, generate, inspect;
- `module` — check, new, including the historical root compatibility action;
- `domain` — check, generate, new;
- `dependency` — check.

C11.6-C owns the detailed command/alias/flag compatibility snapshots. D verifies that the same surfaces are reachable through a real built CLI.

## Diagnostic discovery proof

The qualification binary must prove the canonical C11.6-B flow:

```text
yunka explain YUNKA-DX-CONTRACT-002
```

The text result must retain the canonical Definition identity:

```text
YUNKA-DX-CONTRACT-002
  stage:    contract
  meaning:  generated contract artifacts are stale
  action:   Regenerate: yunka generate
```

The JSON form documented by the C11 happy-path example is also executed exactly as written:

```text
yunka explain YUNKA-DX-CONTRACT-002 --format json
```

Repeated JSON output must be byte-identical and decode to the canonical `pkg/diagnostic.Definition`. Unknown codes must return non-zero and must never receive a fuzzy or invented Definition.

## Qualification execution

`app/cmd/discoverability_qualification_test.go` builds the real CLI with:

```text
go build -buildvcs=true -o <temp>/yunka-c116d ./cmd
```

The E2E runs automatically when `CI=true` and can be reproduced locally with:

```text
YUNKA_REQUIRE_C116D_QUALIFICATION=1 go test ./app/cmd -run TestC116DQualificationRealBinaryDiscoverability -count=1
```

The test records the tracked Git worktree state before the build and requires it to remain identical after all help/explain checks.

## Existing gates retained

C11.6-D does not replace earlier qualification. Exact-head acceptance still requires:

1. Toolchain lock;
2. Dependency drift;
3. Contract reproducibility;
4. Contract compatibility;
5. full `make verify`;
6. Determinism recheck;
7. permanent C11.4 fast-feedback real-binary qualification;
8. permanent C11.5 real dev-runtime qualification;
9. C11.6-C machine-validated examples and expert compatibility snapshots;
10. production verification against MySQL 8.4;
11. clean tracked worktree.

## Invariants

- protobuf remains the canonical writable business contract DSL;
- `framework/operation.Executor` remains the canonical execution runtime;
- root `ExecutionScope` retains UnitOfWork/transaction/idempotency ownership;
- `core.App` retains process lifecycle ownership;
- Project Profile remains location/default metadata only;
- DevManifest remains the explicit local process/readiness/runtime-evidence owner;
- `pkg/diagnostic` remains the stable diagnostic identity owner;
- no reflection DI/service locator is introduced;
- no implicit business semantic inference is introduced;
- no expert command is silently removed, renamed, hidden, or deprecated.

## Done gate

C11.6-D is complete only when the exact PR head passes both normal CI and production qualification, the branch is ahead-only from the current main merge-base, and `main` is advanced by non-forced fast-forward to that exact qualified SHA. After landing, #94 and parent #87 may be closed as completed.
