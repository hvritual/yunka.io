# C4.1 Qualification Status

Canonical publishable-module candidate: `a559be6cde2db03ff28d7e9998a5e5c46020afb6`.

The candidate migrates the publishable module identities to GitHub-backed paths, removes publishable-module local `replace` directives, and removes the repository-owned `github.com/go-kit/kit` compatibility replacement from the release graph.

Qualification requires the exact candidate to pass source-workspace, `GOWORK=off`, and clean external-consumer checks against temporary `v0.1.0-c41rc1` module tags. Failed qualification removes the temporary tags.

Public checksum/proxy propagation is deliberately separated from direct VCS qualification: fresh temporary tags are resolved with `GOPROXY=direct` and `GONOSUMDB=github.com/hvritual/yunka.io/*` so a checksum-cache propagation delay cannot be mistaken for a module-publication defect.
