# C9.9 — Baseline & Conformance Closure

## Status

- State: **Complete and merged to `main`**
- Tracking: GitHub issue #38 — **closed completed**
- Reconciled pre-merge product baseline: `main@407193d0b53f5fdbe2aad5c4ab152aba92d61097`
- Closure PR: #39
- Mechanical non-Draft merge carrier: #40
- Main merge commit: `e091baff2730e04b402710606f655b3a9cd630b7`
- C9.9 absorbed the useful release-closure work previously staged in R0 issue #36 / PR #37 rather than maintaining two parallel closure tracks.

## Objective

Make all authoritative views of the framework describe the same current architecture after C9.8:

```text
main code truth
== generated contract truth
== docs truth
== durable memory truth
== Framework Pressure truth
== issue truth
== real consumer truth
== verification truth
```

C9.9 is not a feature wave. It fixes fact drift, verification infrastructure, generated/consumer drift, and only the smallest code or generated-artifact defect directly proven by a closure gate.

## Final reconciled pressure truth

| ID | Current truth |
|---|---|
| FP-C9-001 | CLOSED by C9.8 canonical internal Operations |
| FP-C9-002 | CLOSED by C9.7 Saga staging through active ExecutionScope transaction |
| FP-C9-003 | CLOSED by C9.7 Executor-owned transaction lifecycle |
| FP-C9-004 | CLOSED for durable duplicate suppression; response replay remains out of scope |
| FP-C9-005 | OPEN / DEFERRED pending repeated Saga-topology pressure |
| FP-C9-006 | CLOSED by C9.8 real cross-Application MySQL proof |

The real `hvritual/biz` Pressure ledger is synchronized to this state.

## Repository and documentation closure

C9.9 reconciled:

- `README.md`: root `ExecutionScope` owns Operation UoW/commit/rollback; `requestscope` supplies typed join views.
- `PROJECT_MEMORY.md`: current governance and C9.7/C9.8 architecture replace superseded local-Git-only and requestscope-root-transaction history.
- `docs/waves/C9-operation-contract-runtime.md`: C9 baseline recorded as complete.
- `docs/waves/C9.7-execution-semantics-closure.md`: FP-C9-001/006 disposition updated to their C9.8 closure state.
- `docs/waves/C9.8-canonical-internal-operations.md`: canonical internal Operation design and final C9.9 revalidation recorded.
- `.github/workflows/production.yml`: permanent read-only PR/push/manual MySQL 8.4 production gate using the locked toolchain and `make verify-production` plus clean-worktree enforcement.
- R0 issue #36 / PR #37: closed as superseded by C9.9.
- C9.8 issue #34: closed with the cross-Application pressure result and final C9.9 evidence.

PR #39 is the durable closure/evidence record. The GitHub Connector's Ready-for-review GraphQL wrapper failed on an unavailable `Repository.fullDatabaseId` field, while GitHub correctly refuses to merge Draft PRs. Therefore non-Draft PR #40 was created from the exact same head/base solely as the merge carrier; it introduced no additional framework files. PR #40 squash-merged the qualified C9.9 tree to `main` as commit `e091baff2730e04b402710606f655b3a9cd630b7`.

## Real consumer final truth

The final real consumer remains the Device Transfer cross-Application slice:

```text
device_transfer (root)
  -> site.validate_transfer_target (internal child Operation)
  -> business decision
  -> device.update (child Operation)
```

Required invariants are now executable facts:

- no fake `ValidateTransferTarget` protobuf RPC;
- no Site REST/gRPC executor registration or generated Site transport executor;
- `site.validate_transfer_target` remains a canonical OperationPlan with empty transport bindings;
- generated child capability uses `operation.ExecuteChildTyped`;
- Device Transfer declares `requiresOperations = [device.update, site.validate_transfer_target]`;
- permission closure is `[device.update, site.read]`;
- one root authorization / one root local UoW is retained by the C9.7 execution model;
- no Application GORM/transaction/authz bypass;
- internal-only DTOs do not leak into external OpenAPI/TypeScript projections unless transport-reachable.

The committed Biz generated truth is the exact 21-file generated state produced by the current compiler. A second `make generate` on that commit is zero-drift.

## Executable evidence

### Framework / production

Run `33242472137` executed the closure commands through a Biz-hosted independent runner because native Yunka jobs were unavailable before step execution:

1. materialized the exact C9.8 merged product tree from a SHA-verified full source artifact plus the merged PR #35 product patch;
2. used locked Go `1.25.13`, protoc `3.21.12`, and locked protobuf Go plugins;
3. ran canonical Yunka contract generation;
4. passed full `make verify-production` against MySQL 8.4;
5. proved the only stale Yunka derived artifacts were deterministic `contracts/generated/client.ts` and `contracts/generated/openapi.json`; those exact generated outputs are now in `main` through C9.9.

The production execution includes the standard architecture/contract/security gates, tests, race, vet, `govulncheck`, build, and MySQL integration.

### Real consumer

The same run `33242472137` then:

- regenerated the final Biz consumer;
- passed internal-Operation/no-external-exposure assertions;
- passed `make verify`;
- passed real MySQL 8.4 `make pressure`.

Run `33244218676` qualified the committed consumer generated truth:

- restored the exact 21 generated files;
- installed the locked compiler/RPC toolchain;
- committed generated files only;
- reran `make generate` and proved zero drift;
- passed final internal-operation/composition assertions;
- passed `make verify`;
- passed MySQL pressure;
- pushed the qualified generated commit.

Permanent Biz `c9-biz-pressure` run `33244429584` passed again after the permanent pressure workflow was updated from the old 5-Operation C9.7 assumptions to the final 7-Operation C9.8/C9.9 truth.

## Native Yunka runner disposition

Fresh and re-run native Yunka `ci` / `production` jobs on this private personal repository repeatedly terminated before any workflow step with `steps=null`. GitHub accepted the workflow definitions and created the jobs, but no checkout/toolchain/test command executed.

C9.9 records this as a repository-hosted-runner infrastructure limitation. It is not counted as passing evidence and is not classified as a code-test failure. Completion uses the stronger executable substitute above: the same canonical locked release commands were executed to completion on the exact merged runtime/product tree with MySQL 8.4, and the exact deterministic generated outputs are committed in `main`.

The permanent native `ci` and `production` workflows remain in the repository for normal PR/main verification when repository-hosted runner capacity is available again.

## Completion gate — final disposition

1. Canonical `make verify`: **PASS** as part of the exact-tree production execution.
2. Canonical `make verify-production` with MySQL 8.4: **PASS**, run `33242472137`.
3. Real Biz regenerate + `make verify` + MySQL pressure: **PASS**, run `33242472137`.
4. Committed consumer generated truth + second generation zero drift + verify + pressure: **PASS**, run `33244218676`.
5. Permanent C9 consumer conformance gate on final Pressure state: **PASS**, run `33244429584`.
6. README, C9/C9.7/C9.8/C9.9 wave docs, Pressure ledger and issues reconciled: **PASS**.
7. C9.8 issue #34: **closed**.
8. R0 issue #36 / PR #37: **closed / superseded**.
9. No new runtime mechanism was introduced by C9.9: **PASS**.
10. Qualified C9.9 closure merged to `main`: **PASS**, PR #40 / commit `e091baff2730e04b402710606f655b3a9cd630b7`.

Therefore **C9.9 is Complete and merged**. C10/new framework-surface work may begin only as a separate pressure-driven wave.

## Non-goals retained

- C10 runtime assembly implementation inside C9.9;
- new ExecutionPolicy mechanisms;
- Saga topology DSL/graph expansion;
- audit/cache policy;
- generic data-scope/SQL DSL;
- workflow/BPMN;
- distributed transaction/2PC;
- reflection DI/service locator;
- business-semantic inference.
