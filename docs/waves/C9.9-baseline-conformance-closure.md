# C9.9 — Baseline & Conformance Closure

## Status

- State: **In progress**
- Tracking: GitHub issue #38
- Branch: `agent/c9-9-baseline-conformance-closure`
- Product baseline under reconciliation: `main@407193d0b53f5fdbe2aad5c4ab152aba92d61097`
- C9.9 absorbs the useful release-closure work previously staged in R0 issue #36 / PR #37 rather than maintaining two parallel closure tracks.

## Objective

Make all authoritative views of the framework describe the same current architecture after C9.8:

```text
main code truth
== generated contract truth
== docs truth
== durable memory truth
== Framework Pressure truth
== issue truth
== real consumer truth
== verification truth
```

C9.9 is not a feature wave. It may fix only fact drift, verification infrastructure, generated/consumer drift, or the smallest code defect directly proven by a closure gate.

## Scope

### 1. Repository baseline truth

- reconcile `README.md`, current wave docs and `PROJECT_MEMORY.md` with C9.7/C9.8 ownership;
- record Root `ExecutionScope` as transaction/UoW owner;
- record `requestscope` as fresh typed repository/scope views that join an active execution UoW under C9.7+ rather than independently owning the root transaction lifecycle;
- document C9.8 canonical internal Operations and external-contract projection.

### 2. Pressure truth

Final expected disposition after evidence is synchronized:

| ID | Current truth |
|---|---|
| FP-C9-001 | CLOSED by C9.8 canonical internal Operations |
| FP-C9-002 | CLOSED by C9.7 Saga staging through active ExecutionScope transaction |
| FP-C9-003 | CLOSED by C9.7 Executor-owned transaction lifecycle |
| FP-C9-004 | CLOSED for durable duplicate suppression; response replay remains out of scope |
| FP-C9-005 | OPEN / DEFERRED pending repeated Saga-topology pressure |
| FP-C9-006 | CLOSED by C9.8 real cross-Application MySQL proof |

The real `hvritual/biz` pressure ledger must be updated to this same state.

### 3. Real consumer truth

Revalidate the real Biz consumer against the exact framework closure tree:

```text
Biz source
  -> generate with current Yunka compiler
  -> zero deterministic drift after committed regeneration
  -> make verify
  -> MySQL 8.4 make pressure
```

Required consumer invariants:

- no fake `ValidateTransferTarget` RPC;
- `site.validate_transfer_target` remains a canonical internal OperationPlan;
- no Site external REST/gRPC registration;
- generated child capability remains `ExecuteChildTyped`;
- one root authorization / one root UoW for Device Transfer;
- no Application GORM/transaction/authz bypass;
- internal-only DTOs do not leak into external OpenAPI/TypeScript projections.

### 4. Verification truth

C9.9 inherits the R0 permanent read-only `production` workflow:

- PRs targeting `main`;
- pushes to `main`;
- manual dispatch;
- locked Go/protoc toolchain;
- MySQL 8.4;
- canonical `make verify-production`;
- clean-worktree enforcement.

Standard `ci` / `make verify` remains the non-MySQL gate.

A workflow job that fails before executing any step (`steps=null`) is an unresolved infrastructure gate. It is neither a pass nor a code-test failure.

## Inherited R0 work

C9.9 adopts, rather than duplicates:

- `.github/workflows/production.yml`;
- compact current-state `PROJECT_MEMORY.md`;
- `AGENTS.md` governance clarification;
- `docs/waves/R0-c9-8-release-closure.md` as historical release-qualification evidence.

R0 issue #36 and PR #37 are now closed as superseded by C9.9; C9.9 is the sole active closure track.

## Executable closure recovery — 2026-08-29

The first C9.9 PR-head `ci` and `production` jobs, plus the first Biz source-export job, all initially ended before any step executed (`steps=null`). Re-running the Biz export later recovered successfully on the same exact Biz head `0acc07819c434cbd3367316ebd684a9309e3cd46`:

- Biz run `33208365338`, re-run job: **SUCCESS**;
- checkout, source payload construction and artifact upload all executed successfully;
- artifact `biz-c9-pressure-source` id `9705875694`, SHA-256 digest `2c892fbbbed7fd019f1215204b334d1f22d0b0f726c3a97d4e725b0e58cc84c4`.

The old Yunka `ci` and `production` run instances still ended at `steps=null` when re-run, so C9.9 no longer treats re-running those historical instances as useful evidence. This documentation commit intentionally advances the PR head and triggers fresh `ci` / `production` runs on a new exact head.

Locally, the real Biz source artifact confirms the intended final handwritten consumer state before regeneration:

- `SiteApplication` declares `site.validate_transfer_target` under `ApplicationDeclaration.operations` and has no RPC method;
- `module.go` has no Site REST/gRPC executor registration;
- handwritten cross-Application code depends on generated `DeviceopsSiteManagementChildCapability`;
- committed generated artifacts are intentionally stale and therefore must be regenerated before the consumer can qualify.

The generated-state closure must therefore be proven by regeneration, not by treating the current committed Biz generated files as authoritative.

## Completion gate

C9.9 is complete only when all of the following are true on the final closure tree:

1. `make verify` is green.
2. `make verify-production` is green with MySQL 8.4.
3. Real Biz regenerate + `make verify` + MySQL pressure is green against the same framework tree.
4. Generated artifacts and worktrees are clean.
5. README, wave docs, PROJECT_MEMORY, Pressure ledger and GitHub issues contain no known superseded C9.7/C9.8 status.
6. C9.8 issue #34 is closed with final evidence.
7. R0 issue #36 / PR #37 are closed as superseded by C9.9.
8. No new framework surface was added unless an executable closure gate proved it necessary.

## Non-goals

- C10 runtime assembly work;
- new ExecutionPolicy mechanisms;
- Saga topology DSL/graph expansion;
- audit/cache policy;
- generic data-scope/SQL DSL;
- workflow/BPMN;
- distributed transaction/2PC;
- reflection DI/service locator;
- business-semantic inference.
