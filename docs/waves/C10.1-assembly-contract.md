# C10.1 — Assembly Contract

## Status

- State: **Complete / qualified**
- Parent: #42 — C10 Runtime Assembly & Framework Productization
- Tracking: #44 / Draft PR #45
- Qualified exact head: `e9c54e9657ffc64d0e56bcd869d9a7bf61e72f7c`
- Executable qualification: `hvritual/biz` run `33278311314`, job `99168895743`

The code head is frozen. Qualification does not authorize merge or movement of `main`.

## Delivered contract

C10.1 established the leaf-safe immutable `pkg/assemblyplan` IR and the one-way ownership projection from already canonical facts.

AssemblyPlan records only derived/reused assembly facts:

- Domain/Application identity and `requires` from the protobuf-derived Contract Manifest;
- Operation identity, internal dependencies, execution facts and explicit binding references from the canonical OperationPlan;
- static module identity/dependency/capability requirements from immutable module descriptor snapshots;
- one deterministic root bootstrap target;
- provenance/evidence for all entities, edges and references.

It does **not** own HTTP methods/paths, gRPC full methods, process argv/workdir/env/readiness, runtime health observations, secrets, builders, providers or service values.

## Permanent invariants

- identical canonical inputs produce identical canonical JSON bytes and SHA-256 digest;
- unknown references, duplicate identities, cycles, stale closures/requirements/targets and stale Manifest/OperationPlan joins fail closed;
- internal Operations remain transport-free unless an explicit external binding exists;
- module runtime builders/providers never enter committed AssemblyPlan truth;
- runtime-local evidence is rejected from the committed plan;
- `pkg/assemblyplan` remains data/IR and never becomes a registry, service locator or runtime owner.

## Exact qualification

Run `33278311314` / job `99168895743` asserted exact checkout identity and used the locked environment:

- Go `1.25.13`;
- protoc `3.21.12`;
- protoc-gen-go `1.36.11`;
- protoc-gen-go-grpc `1.6.2`;
- MySQL `8.4.11`.

The exact C10.1 head passed `make verify-production`, architecture/contract/RPC/test/race/vet/govulncheck/build/MySQL integration gates and final tracked-worktree cleanliness.

## Boundary

C10.1 added no generated runtime bootstrap, authorization/Executor change, transaction/idempotency change, server/process lifecycle, dev-runtime behavior or consumer migration. Those responsibilities remain owned by the later qualified C10 waves.
