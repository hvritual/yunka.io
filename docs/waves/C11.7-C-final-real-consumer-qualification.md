# C11.7-C — Final Real Consumer Qualification

Tracks #101 and parent #96.

## Qualified input pair

- Yunka: `main@d9bc214a0485a48fefae0826051142d741e54257`
- Biz: `main@df3a8ed8d0bf1c1eb0e75889675bd7291b0dd1d8`

## Purpose

C11.7-C is a qualification-only wave. It adds no compiler, runtime, contract, security, transaction, idempotency, diagnostics, readiness, or lifecycle semantics. Its only purpose is to prove that the complete C11 developer workflow works from clean checkouts against the real Biz consumer while preserving C9/C10 execution semantics.

## Final consumer path

```text
clean Yunka candidate checkout
        +
clean qualified Biz checkout
        ↓
locked Go / protoc / rpc tools
        ↓
repository-contained protobuf dependencies
        ↓
yunka generate × 2
        ↓
yunka check
        ↓
real zero-argument yunka dev
        ↓
real cmd/biz Ready
        ↓
canonical diagnostics + three-Application runtime closure
        ↓
graceful process-tree shutdown
        ↓
C9/C10 verify + MySQL 8.4 pressure
        ↓
clean Yunka and Biz worktrees
```

## Required evidence

1. Both repositories are checked out by exact commit SHA; no branch-name resolution is accepted as qualification evidence.
2. Biz's pinned `yunka/dsl/v1/options.proto` is byte-identical to the Yunka candidate contract dependency.
3. The first and second C11 happy-path generations produce identical generated-file digests and zero tracked drift.
4. `yunka check` is read-only and leaves the Biz worktree clean.
5. OperationPlan SHA256 remains `903905a8bea81901f9682f487809e57d635991b0689c36c1aee9c08a990ee35a`.
6. AssemblyPlan SHA256 remains `1d2d31c9b0c723aaadaede2ed0eb8d627a1684a74f1c5988b1b4d1468cdb5514`.
7. A VCS-stamped Yunka CLI starts the real Biz process through bare `yunka dev`.
8. Runtime evidence reaches Ready only through the existing DevRuntime readiness/diagnostics path.
9. `yunka dev status --closure --format json` proves one `biz` process owns the three canonical Application nodes.
10. Runtime graph contains exactly three `process:biz -> Application` `runs` edges.
11. After SIGTERM of the Yunka supervisor, the runtime report is stopped and the diagnostics endpoint is no longer reachable, proving no orphan Biz child remains.
12. Existing Biz `verify` and MySQL 8.4 pressure gates pass.
13. Both Biz and Yunka tracked worktrees remain clean.
14. The same Yunka exact head independently passes repository CI/Determinism and production/MySQL 8.4.

## Non-goals

- no test helper process in place of `cmd/biz`;
- no new project-profile or DevManifest fields;
- no new process supervisor or readiness model;
- no new diagnostics schema;
- no changes to protobuf, OperationPlan, AssemblyPlan, `framework/operation.Executor`, root `ExecutionScope`, authorization, transaction/idempotency, generated assembly ownership, or `core.App` lifecycle.

## Landing policy

The final Yunka candidate must be ahead-only from the qualified base, with `behind_by=0` and the current main as exact merge-base. Landing is a non-forced fast-forward so the SHA that passed all gates is the SHA that becomes `main`.

C11.7-C is not complete until the final consumer workflow, Yunka CI/Determinism, and production/MySQL 8.4 all pass on the same exact candidate head.