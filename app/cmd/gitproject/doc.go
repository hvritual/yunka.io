// Package gitproject translates between Yunka project-relative evidence paths
// and Git repository-relative tree paths. The mapping is derived from the
// checkout and is not persisted as another project source of truth.
package gitproject
